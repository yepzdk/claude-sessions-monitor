## Description

<!-- What does this change do, and why? -->

## How was this tested?

<!-- Commands run, or manual steps taken. -->

## Screenshots

<!-- Changed the web dashboard or the terminal UI? Before and after, one image
     each. Delete this section if the change is not visible. -->

## Checklist

- [ ] `make check` passes (gofmt, vet, build, tests — what CI runs)
- [ ] Touched a `_darwin.go` / `_linux.go` file? `GOOS=darwin go vet ./...` and `GOOS=linux go vet ./...` both pass
- [ ] `CHANGELOG.md` updated under `[Unreleased]`
- [ ] Changed `internal/web/static/` or `internal/ui/`? Before/after screenshots are in the description
- [ ] README/docs updated if behaviour or configuration changed
